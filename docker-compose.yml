front:
  build: images/nginx
  ports:
    - "80:80"
  volumes:
    - www:/var/www
    - sites:/etc/nginx/sites-enabled
  links:
    - mysql:mysql

mysql:
  build: images/mariadb
  ports:
    - "3306:3306"